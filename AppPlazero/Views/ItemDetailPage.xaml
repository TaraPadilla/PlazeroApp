<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AppPlazero.Views.ItemDetailPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15">
        <Label Text="Nombre" FontSize="Medium"/>
        <Entry Text="{Binding Item.Nombre}" Placeholder="Nombre Producto"
                                            IsTextPredictionEnabled="True"
                                            MaxLength="20"/>
        <Label Text="Cantidad" FontSize="Medium" d:Text="Item description"/>
        <Entry Text="{Binding Item.pro_fCantidadAct}"/>
        <Label Text="Unidad Medida" FontSize="Medium"/>
        <Entry Text="{Binding Item.pro_nidUnidadBase}"  
                              Placeholder="Unidad primaria de medida" 
                              Keyboard="Numeric" 
                              MaxLength="1"/>
         <Button Text="Guardar" Clicked="OnGuardarClicked" />
    </StackLayout>
    
</ContentPage>